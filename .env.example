# Bu dosya örnektir; gerçek değerleri .env.local'e yaz.
# .env* dosyaları .gitignore'da — depoya girmezler.

# Neon PostgreSQL bağlantı dizesi.
# Neon panelinde: Project > Connection Details > Connection string
# Havuzlanmış (pooled) uç noktayı kullan — serverless fonksiyonlar için
# bağlantı başına açılıp kapanmayı kaldırabilecek olan odur.
DATABASE_URL=
